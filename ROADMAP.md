# Roadmap

Forward-looking work not yet on `main`. Each entry carries a
**Motivation** (why), a **Sketch** (rough implementation path), and a
**Complexity** estimate (Small / Medium / Large).

Nothing is currently queued — the SAML 2.0 binding matrix
(Redirect / POST / Artifact + SOAP back-channel, HTTP-Artifact,
Holder-of-Key, and ECP/PAOS), federation aggregate
(`<md:EntitiesDescriptor>`) support, and CI coverage reporting are all
on `main`. New items get appended here as they come up.
