<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Error — saml-idp-example</title>
  <link rel="stylesheet" href="/static/style.css">
</head>
<body>
  <main class="shell">
    <header class="brand">
      <div class="mark">R</div>
      <div class="name">saml-idp-example <span>· error</span></div>
    </header>

    <section class="hero">
      <span class="kicker" style="background: rgba(138, 31, 31, 0.08); color: var(--danger);">
        <span class="dot" style="background: var(--danger);"></span> Request failed
      </span>
      <h1>Something went sideways.</h1>
      <p class="lede">{{message}}</p>
      <a class="btn btn-primary" href="/">Back to start</a>
    </section>
  </main>
</body>
</html>
